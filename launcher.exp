#! /usr/bin/expect -f
set anki_version [lindex $argv 0]

spawn anki

expect "2\)"
send -- "2\r"

expect "$anki_version"
send -- "$anki_version\r"

expect "Press enter to start Anki."

# It's not necessary to start Anki at this point
close -i $spawn_id
wait -i $spawn_id
